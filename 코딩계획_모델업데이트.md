# 코딩 계획 - Gemini 모델 업데이트

## 1. 문제 요약
- 현재 [app.py:152](app.py#L152) 에서 `gemini-2.0-flash` 모델 사용 중
- 이 모델은 **신규 사용자에게 더 이상 제공되지 않음** (Google 정책 변경)
- 에러 코드: `404 NOT_FOUND` / `models/gemini-2.0-flash is no longer available to new users`
- 새로 발급한 API 키는 신규 사용자 자격이라 구모델 접근 차단됨

## 2. 해결 방안
`gemini-2.0-flash` → **`gemini-2.5-flash`** 로 모델 이름 교체.

### 왜 `gemini-2.5-flash` 인가?
| 항목 | 설명 |
|------|------|
| **멀티모달 지원** | 이미지(PDF/JPG/PNG) 입력 처리 가능 — 본 앱의 핵심 요구사항 |
| **속도** | Flash 계열이라 응답 빠름 (10–20초 분석 spinner와 부합) |
| **비용** | Pro 대비 저렴, Flash 계열 중 최신 |
| **신규 키 호환성** | 2026년 4월 현재 신규 사용자에게도 정상 제공 |
| **출력 토큰** | 8192 토큰 출력 가능 (현재 `max_output_tokens=8192` 그대로 호환) |

### 다른 후보 검토
- `gemini-flash-latest` : "최신 별칭"이지만 자동 변경되어 출력 형식이 흔들릴 위험 → 제외
- `gemini-2.5-pro` : 더 강력하지만 비용 ↑, 속도 ↓, 본 앱은 Flash로 충분 → 제외

## 3. 변경할 파일
**[app.py:152](app.py#L152)** 한 줄만 수정:
```diff
- model='gemini-2.0-flash',
+ model='gemini-2.5-flash',
```

이 외에는 변경 없음. `client.models.generate_content` API 시그니처, `GenerateContentConfig`, `temperature`, `max_output_tokens` 모두 동일하게 호환됩니다.

## 4. 검증 방법
1. Streamlit Cloud에서 자동 재배포 (GitHub 푸시 시) 또는 수동 재배포
2. `Sumin Yoon-DSAT-01-B.jpg` 업로드 → "🚀 Generate Analysis Report" 클릭
3. 정상적으로 분석 리포트가 생성되면 성공
4. 만약 또 다른 에러가 나오면 `check_models.py`를 활용해 본인 키로 사용 가능한 모델 목록을 직접 확인

## 5. 추가 권장사항 (선택)
지금 당장은 적용하지 않고, 다음번에 고려:
- **모델 이름을 환경변수/상수로 분리** → 추후 모델 교체 시 코드 수정 없이 가능
- **fallback 로직** → 한 모델이 실패하면 다음 모델로 자동 재시도

이번에는 **최소 변경 원칙**으로 한 줄만 고치겠습니다.
