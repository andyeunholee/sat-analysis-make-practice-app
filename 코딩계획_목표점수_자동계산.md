# 코딩 계획 — Target Score 자동 계산 (최고점 +50~100, 최대 1600)

## 목표
리포트의 **TARGET SCORE** 카드(빨간 원 위치)에 표시되는 목표 점수를,
AI가 임의로 정하는 값이 아니라 **학생의 SAT 시험 결과 중 가장 높은 총점 기준 +50 ~ +100점**으로
자동 계산해서 표시한다. 단, SAT 만점인 **1600점을 넘지 않는다.**

## 현재 상태 (문제점)
- `app.py:1423` 프롬프트에서 Target Score를 `knowledge/sat_curriculum.json`의
  `scoring_guidelines` 티어 표(1540-1600 / 1300-1400 / 1200-1250)로 정하게 되어 있음.
- 그 결과 실제 최고점이 1000점대여도 **1550** 같은 비현실적인 목표가 표시됨(첨부 이미지 사례).
- `app.py:879` `_parse_report()`가 AI 텍스트의 `Target Score:` 줄을 그대로 읽어
  `app.py:1123` `_stat_card(..., 'TARGET SCORE', ...)` 카드에 출력.

## 계산 규칙
1. 최고 총점(peak) = 리포트의 ```scores``` 블록 `total` 값들의 최댓값
   (없으면 `Test Trajectory:` 줄에서 파싱한 점수들의 최댓값).
2. 하한 = peak + 50, 상한 = peak + 100.
3. 두 값 모두 1600으로 상한 처리(clamp).
4. 표시 형식
   - 하한 < 상한 → `"1050 - 1100"` (범위 표시)
   - 하한 == 상한(= 둘 다 1600) → `"1600"` (단일 값)
5. peak를 구할 수 없으면 기존 AI 값을 그대로 사용(폴백, 예외 없음).

### 예시
| 최고 점수 | 표시되는 Target Score |
|---|---|
| 930 | 980 - 1030 |
| 1000 | 1050 - 1100 |
| 1520 | 1570 - 1600 |
| 1560 | 1600 |

## 변경 작업 (app.py)
1. **신규 함수 `_compute_target_score(peak)`** — 위 규칙대로 문자열 반환.
2. **신규 함수 `_peak_total_from_report(md)`** — 리포트 텍스트에서 최고 총점 추출
   (scores 블록 우선, 실패 시 Test Trajectory 파싱).
3. **신규 함수 `enforce_target_score(md)`** — 리포트 마크다운의
   `**Target Score:** ...` 줄을 계산값으로 치환. 화면 표시 리포트와 Word 리포트가
   동일한 값을 갖도록 리포트 생성 직후(`app.py:1462` 부근)에 1회 적용.
4. **`_parse_report()` 보정** — 파싱 후에도 peak가 있으면 `target_score`를 계산값으로 덮어씀
   (기존 세션·예전 리포트에서도 Word 카드가 규칙을 따르도록 하는 안전장치).
5. **프롬프트 수정 (`app.py:1423`)** — Target Score를 "가장 높은 총점 +50~100, 최대 1600"으로
   산출하도록 지시하고, `Goal Breakdown`의 Math/RW 목표 점수도 그 목표 총점과
   합이 맞도록 지시(카드 하단 `Math 700-750 · RW 600-650` 문구 일관성 확보).

## 영향 범위
- 화면 리포트 본문의 `Target Score` 줄
- Word 다운로드 리포트의 `TARGET SCORE` 통계 카드
- 그 외 주차별 계획·약점 분석 로직은 변경 없음

## 검증
- 파이썬 문법 검사(`py_compile`)
- 규칙 함수 단위 확인: 930 / 1000 / 1520 / 1560 / 값 없음 케이스
