# 코딩 계획: 리포트 중간 잘림(MAX_TOKENS) 수정

## 문제
- Gemini가 생성한 분석 리포트가 "Week 2 ... Chapter 17 (Measures of Center" 에서 잘림
- 원인: `max_output_tokens=8192` 한계 초과
- Gemini 2.5 Flash 최대 출력: 65,536 토큰

## 수정 계획

### 1. `app.py` `get_gemini_response()` 함수 수정

**A. max_output_tokens 증가**
- `8192` → `32768` 로 변경 (안정성과 비용 사이의 균형, 약 24,000 영문 단어 분량)

**B. 잘림 감지 및 경고 추가**
- API 응답 후 `finish_reason` 확인
- `MAX_TOKENS`(=2)로 잘렸다면 화면에 명확한 경고 표시
- 부분 응답이라도 반환은 하되, 사용자에게 알림

**C. 디버깅용 키 끝 4자리 표시 제거 (선택)**
- 원래대로 `✅ API Key Loaded` 로 복구
- (사용자 확인 후 진행)

### 2. 검증
- Streamlit 자동 재시작 (소스 변경 감지)
- 사용자가 다시 분석 실행
- 전체 8주 학습 계획까지 모두 표시되는지 확인

## 기대 효과
- 8주 분량의 전체 리포트가 잘리지 않고 출력
- 만약 또 잘리면 사용자에게 경고가 표시되어 즉시 인지 가능
